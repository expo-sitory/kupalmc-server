name: Ixpu-WorldEvents
author: [Ixpu]
version: '${project.version}'
main: dev.ixpu.worldevents.WorldEvents
api-version: 1.21
description: World event management system
commands:
  we:
    description: WorldEvents plugin commands
    usage: /we reload or /we status